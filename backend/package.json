{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "esbuild": "esbuild ./src/main.ts --bundle --minify --watch --platform=node --main-fields=module,main --outfile=bundle.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.3.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.11.7",
    "esbuild": "^0.13.13",
    "nodemon": "^2.0.15",
    "typescript": "^4.4.4"
  }
}
